name: jolt_surge
description: A lightweight reactive state container for Flutter, built on Jolt Signals and inspired by BLoC's Cubit architecture.

version: 0.0.1-beta.0

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  jolt: ^0.0.6
  provider: ^6.1.5+1
  shared_interfaces: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
