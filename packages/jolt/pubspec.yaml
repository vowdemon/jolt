name: jolt
description: A lightweight reactive state management library built on alien_signals, focused on simple and efficient state handling.
version: 0.0.4
repository: https://github.com/vowdemon/jolt

environment:
  sdk: ^3.0.0

dependencies:
  path: ^1.9.0
  meta: ^1.0.0
  shared_interfaces: ^0.0.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

