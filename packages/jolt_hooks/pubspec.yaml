name: jolt_hooks
description: "Flutter hooks for jolt"

repository: https://github.com/vowdemon/jolt

topics:
  - jolt
  - state-management
  - signals
  - hook

version: 0.0.5+1

resolution: workspace

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: ^0.21.3+1
  jolt: ^0.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  jolt_flutter: ^0.0.6

  flutter_lints: ^5.0.0
