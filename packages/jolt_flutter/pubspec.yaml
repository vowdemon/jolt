name: jolt_flutter
description: "A Flutter extension of jolt that provides reactive UI support, enabling automatic synchronization between state and interface."
version: 0.0.5
repository: https://github.com/vowdemon/jolt

topics:
  - jolt
  - state-management
  - signals
  - flutter

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  free_disposer: ^1.1.3
  jolt: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

